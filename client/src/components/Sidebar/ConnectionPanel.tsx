/**
 * Sidebar connection panel: profile dropdown, connect/disconnect buttons,
 * "Manage Connections" expander.
 *
 * Owned by the connection lane. See workflow contract.
 */
export function ConnectionPanel() {
  return (
    <div className="text-sm text-slate-500">ConnectionPanel — not implemented</div>
  );
}
