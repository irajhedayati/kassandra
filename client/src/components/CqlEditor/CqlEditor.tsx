/**
 * Monaco-based CQL editor + execute button. Renders results below
 * (DataFrame-style for SELECT, success/error toast for DDL/DML).
 *
 * Owned by the cql lane.
 */
export function CqlEditor() {
  return (
    <div className="px-6 py-3 text-sm text-slate-500">CqlEditor — not implemented</div>
  );
}
